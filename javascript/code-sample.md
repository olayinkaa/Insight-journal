### SAMPLE CODES
```js
function sum(...numbers){
    return numbers
}
sum([2,4,4],[5,6,7]) // [[2,4,4], [5,6,7]]
```
```js
const newArr = nums.concat(arr1, arr1);
newArr.fill(2,2)
Array.flat(2) // depth of 2
```
```js
str.charCodeAt(0) // return the char code of letter
String.fromCharCode(65) // to get the ASCII code 
str.includes("hello")
str1.localeCompare(str2) // if equal return 0, if not equal return -1
str.split(" ") // return the string in array
arr.join("") // join array of string
```
```js
let num = 2;
num.toString(2) // convert num to binary
```