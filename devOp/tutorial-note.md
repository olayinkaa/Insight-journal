```shell
touch newfile.txt
rm newfile.txt
mkdir test
rm -r test

find path --name "newfile.txt"
grep 
```
```md
shell script is a text file where series of commands is written that the linux shell (Bash) will execute

chmod +x variable.sh
```
```ts
#!/bin/bash
name="Sangam"
echo "Hello, $name"

number=5
if [ $number -gt 5 ]; then
    echo ""
else
    echo ""
fi
```
```md
GIT distributed version control
- track changes in your source code
Guthub - cloud based hosting service
``

## What is DevOP ?
Set of practices that combine software development aim at automating and improving the flow of development,
testing and deployment.
Help in build faster, deploy safer, easy to maintain
plan-> Develop-> build-> test -> release -> deploy -> monitor